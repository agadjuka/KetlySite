# Модуль Velvet SPA

Модуль Velvet SPA полностью подготовлен к копированию и созданию новых агентов на его основе.

## Что было сделано

1. **Создан конфигурационный файл** (`config.ts`) - все настройки модуля вынесены в один файл
2. **Рефакторинг модуля** - все файлы модуля теперь используют конфиг вместо хардкода
3. **Модульность** - модуль полностью независим и готов к копированию

## Структура модуля

```
velvet-spa/
├── assets/
│   └── logos/          # Логотипы агента
├── chat/               # (пустая папка для будущих файлов)
├── components/         # Компоненты Google Sheets
├── layouts/            # Макеты для desktop и mobile
├── tour/               # Компоненты тура
├── widgets/            # Виджеты агента
├── config.ts           # ⭐ Конфигурация модуля
├── getConfig.ts        # Экспорт конфига
├── index.ts            # Экспорты модуля
├── page.tsx            # Главная страница агента
├── COPY_INSTRUCTIONS.md # 📖 Подробная инструкция по копированию
└── README.md           # Этот файл
```

## Как использовать

Для создания нового агента на основе Velvet SPA:

1. **Скопируйте папку** `src/modules/velvet-spa` в новую папку (например, `src/modules/restaurant`)
2. **Следуйте инструкции** в файле `COPY_INSTRUCTIONS.md`
3. **Обновите конфиг** в файле `config.ts` новой папки
4. **Переименуйте файлы** согласно инструкции
5. **Обновите внешние файлы** (список в инструкции)

## Главный файл для изменения

**`config.ts`** - это единственный файл, который нужно изменить для базовой настройки нового агента. Все остальные файлы модуля автоматически используют эти настройки.

## Важно

- Все настройки модуля находятся в `config.ts`
- При копировании модуля нужно будет обновить только конфиг и переименовать файлы
- Внешние файлы (например, `ExamplesButton.tsx`) нужно будет обновить вручную для добавления ссылки на нового агента

## Подробная инструкция

См. файл `COPY_INSTRUCTIONS.md` для пошаговой инструкции по копированию модуля.


